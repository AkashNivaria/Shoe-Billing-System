CREATE TABLE `customers` (
  `Bill_no` int DEFAULT NULL,
  `C_name` varchar(20) DEFAULT NULL,
  `C_mob` varchar(12) DEFAULT NULL,
  `P_code` int DEFAULT NULL,
  `Qty` int DEFAULT NULL
) 

select * from customers;